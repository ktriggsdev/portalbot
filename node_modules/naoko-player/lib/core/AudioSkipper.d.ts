/// <reference types="node" />
/// <reference types="node" />
import internal from "node:stream";
declare class AudioSkipper extends internal.Writable {
    source: internal.Readable;
    _bytes: number;
    constructor(source: internal.Readable, skipSeconds?: number);
    _transform(chunk: any, encoding: BufferEncoding, callback: internal.TransformCallback): void;
}
export = AudioSkipper;
