import { ITrack } from "../provider/Provider";
import MusicManager from "./MusicManager";
declare class MusicNode {
    readonly guildId: string;
    readonly manager: MusicManager;
    private _player;
    private _resource;
    private _audio;
    constructor(guildId: string, manager: MusicManager);
    get connection(): import("@discordjs/voice").VoiceConnection;
    playTrack(track: ITrack): Promise<void>;
    skipForward(seconds: number): Promise<boolean>;
    stop(): boolean;
    private _createAudioResource;
}
export = MusicNode;
