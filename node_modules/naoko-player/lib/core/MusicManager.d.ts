import { joinVoiceChannel } from "@discordjs/voice";
import { Provider } from "../provider/Provider";
import MusicNode from "./MusicNode";
interface ManagerOptions {
    providers: Provider[];
    cacheEnabled?: boolean;
}
declare class MusicManager {
    options: ManagerOptions;
    private nodes;
    providers: Map<string, Provider>;
    constructor(options: ManagerOptions);
    createNode(guildId: string, force?: boolean): MusicNode;
    getNode(guildId: string, forceCreate?: boolean): MusicNode;
    /**
     * Creates a VoiceConnection to a Discord voice channel.
     *
     * @param options the options for joining the voice channel
     */
    get joinVoiceChannel(): typeof joinVoiceChannel;
}
export = MusicManager;
